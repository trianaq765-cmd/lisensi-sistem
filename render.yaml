services:
  - type: web
    name: license-system
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: license-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: API_KEY
        generateValue: true
